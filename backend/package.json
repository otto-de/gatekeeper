{
  "name": "backend",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "node ./src/bin/www",
    "production": "cd ../frontend && yarn --production=false && yarn run build && yarn start",
    "test": "jest"
  },
  "dependencies": {
    "body-parser": "~1.17.1",
    "cookie-parser": "~1.4.3",
    "debug": "~2.6.3",
    "express": "~4.15.3",
    "express-validation": "^1.0.2",
    "joi": "^10.6.0",
    "monk": "^6.0.1",
    "morgan": "~1.8.1",
    "serve-favicon": "~2.4.2"
  },
  "devDependencies": {
    "jest-cli": "^20.0.4",
    "supertest": "^3.0.0"
  }
}
